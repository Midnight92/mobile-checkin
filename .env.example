# Copy to .env and adjust
PORT=3000
SESSION_SECRET=change_me_to_a_long_random_string
ADMIN_USER=admin
ADMIN_PASS=changem

# Minutes before cookie expiry is refreshed (user activity extends it)
SESSION_MINUTES=120
# Admin idle auto-logout (front-end timer, minutes)
# (Set this in a small <script> on admin.html if you need to override)
