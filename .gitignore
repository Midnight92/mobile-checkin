# Dependencies
node_modules

# Environment variables
.env

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS Finder files
.DS_Store

# SQLite local DBs (not used in deploy branch but exists in dev)
data.db
sessions.db
